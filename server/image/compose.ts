// Part composition — port of compose_parts.py (sharp).
//
// STUB: implemented by a later track. Lays out generated part PNGs from
// `partsDir` onto a single canvas per the parts_layout config.

/** Compose part PNGs from `partsDir` into a single sheet per `config`. */
export async function composeParts(config: unknown, partsDir: string): Promise<Buffer> {
  void config;
  void partsDir;
  throw new Error("not implemented: compose track (compose_parts.py port)");
}
