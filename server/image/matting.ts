// Triangulation matting — port of matting.py (sharp).
//
// STUB: implemented by a later track. Given the same subject rendered on white
// and on black backgrounds, recovers a straight-alpha transparent PNG.

/** Recover a transparent PNG from white-bg and black-bg renders of a subject. */
export async function triangulationMatting(whitePng: Buffer, blackPng: Buffer): Promise<Buffer> {
  void whitePng;
  void blackPng;
  throw new Error("not implemented: matting track (matting.py port)");
}
